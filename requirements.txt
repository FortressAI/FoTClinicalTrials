# Field of Truth Clinical Trials System - Minimal Cloud Requirements
# Ultra-minimal dependencies for Streamlit Cloud deployment

# Core Streamlit and data processing (essential only)
streamlit>=1.28.0
pandas>=2.0.0
numpy>=1.24.0
plotly>=5.15.0

# Basic scientific computing
scipy>=1.10.0

# Data validation
pydantic>=2.0.0

# Web requests
requests>=2.31.0

# Security
cryptography>=41.0.0

# Logging
structlog>=23.1.0

# Remove all heavy dependencies that cause cloud issues:
# qiskit>=0.44.0  # Heavy quantum library - causes memory issues
# scikit-learn>=1.3.0  # Heavy ML library - causes memory issues
# matplotlib>=3.7.0  # Heavy plotting library - causes memory issues
# seaborn>=0.12.0  # Heavy plotting library - causes memory issues
# jsonschema>=4.17.0  # Optional validation
# sqlalchemy>=2.0.0  # Database ORM - not needed for cloud
# rdkit-pypi>=2023.3.1  # Heavy chemistry library - causes memory issues
# numba>=0.57.0  # Heavy JIT compiler - causes memory issues
# pytest>=7.4.0  # Testing framework - not needed for production
# sphinx>=7.1.0  # Documentation - not needed for production